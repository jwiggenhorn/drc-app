const API_URL = __DEV__ ? 'http://localhost:3000/api' : '[Production API URL]'
const IMAGES_URL = __DEV__
  ? 'http://localhost:3000/images'
  : '[Production IMAGES URL]'

export { API_URL, IMAGES_URL }
